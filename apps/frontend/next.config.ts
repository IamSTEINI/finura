import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
	devIndicators: false,
	allowedDevOrigins: ["https://finura-production.up.railway.app"],
};

export default nextConfig;
